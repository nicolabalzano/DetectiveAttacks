nei mapping con attack l'interfaccia è buggata

bisogna mettere un tasto per il mapping tramite AI per vulnerabilità cercate nella UI

il mapping deve essere automatico ma l'esperto deve confermare il mapping manualmente per ogni vulnerabilità, per non fidarsi dell'AI

deployare il modello localmente

cambiare il prompt dando la descrione estesa

integrare la roba di lorenzo, vedere se si riesce ad ottimizzare la ricerca ancora di più